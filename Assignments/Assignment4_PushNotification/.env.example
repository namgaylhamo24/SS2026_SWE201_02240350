# Optional — API URL for physical device (use your PC LAN IP, not localhost)
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
EXPO_PUBLIC_ADMIN_API_KEY=dev-admin-key-change-me

# projectId lives in app.json → expo.extra.eas.projectId (see README)

# Backend (copy to backend/.env)
PORT=3000
API_KEY=dev-admin-key-change-me
